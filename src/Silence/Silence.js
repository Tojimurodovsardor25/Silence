import React from "react";

const Silence = (props) => {
    return (
        <div className="Container">
            <header className="App-header">
            
            </header>
        </div>
    );
}

export default Silence;
